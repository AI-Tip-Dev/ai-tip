{
  "name": "ai-tip",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter electron-ai-sidebar dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  }
}
